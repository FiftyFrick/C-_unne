#include <stdio.h>

int main () {
	int a = 5;
	int b = 17;
    if ((a > b) && (a > 0)) {
	 	printf("%d>%d y %d > 0 es VERDADERO\n", a, b, a);
	 }else {
	    printf("%d > %d y %d > 0 es FALSO\n", a, b, a );
    }
	return 0; 
	
}
	
